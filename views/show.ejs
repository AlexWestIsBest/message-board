<%- include(`./partials/head.ejs`) %>
<body>
    <h1>View Post</h1>
    <fieldset>
        <% if (m.name.length !== 0) { %>
            <legend><%= m.name %></legend>
        <% } %> 
        <% if (m.image.length !== 0) { %>
            <img src="<%= m.image %>" width="100%">
        <% } %>
        <% if (m.text.length !== 0) { %>
            <div><%= m.text %></div>
        <% } %>
    </fieldset>
    <div class="buttonArray">
        <a href="javascript:history.back()"><img class="icon" src="https://cdn-icons-png.flaticon.com/128/5166/5166297.png" alt="Home"></a>
        <a href="/newsfeed/<%= m._id %>/edit"><img class="icon" src="https://cdn-icons-png.flaticon.com/128/5166/5166607.png" alt="Edit"></a>
        <form action="/newsfeed/<%= m._id %>?_method=DELETE" method="POST">
            <input type="image" class="icon" src="https://cdn-icons-png.flaticon.com/128/5166/5166625.png" alt="Delete">
        </form>
    </div>
</body>
</html>